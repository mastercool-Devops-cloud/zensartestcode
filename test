int mappedPort = oracleContainer.getFirstMappedPort();
    String jdbcUrl = String.format("jdbc:oracle:thin:@localhost:%d/testDB", mappedPort);

    // Register the properties dynamically
    registry.add("spring.datasource.url", () -> jdbcUrl);
