 try (MockedStatic<LoggerFactory> mockedStaticLoggerFactory = Mockito.mockStatic(LoggerFactory.class)) {
            mockedStaticLoggerFactory.when(() -> LoggerFactory.getLogger(FileUtils.class)).thenReturn(mockLogger);
