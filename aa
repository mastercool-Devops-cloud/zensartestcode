
"query", Optional.ofNullable(req.getQueryString()).orElse(""),
                        "headers", Collections.list(req.getHeaderNames())
                                .stream()
                                .collect(Collectors.toMap(h -> h, req::getHeader)),
                        "body", getRequestBody(req)
